# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/

# Local dev database (real Postgres data files — never commit these)
backend/.pgdata/

# Build output — the SPA is built into the image, not committed
backend/static/
frontend/dist/

# Node
node_modules/
*.tsbuildinfo

# Env — see SETUP.md. backend/.env is written by `make db`.
.env
.env.*
!.env.example

# OS / editor
.DS_Store
.idea/
.vscode/
*.pem
